@model IEnumerable<BibliotecaNA.Models.Domain.Usuario>
@{
}

<div class="container pt-5">
    <div class="row">
        <h3 class="pink-tlt">Usuarios</h3>
        <table class="table table-borderless table-hover">
            <thead>
                <tr>
                    <th>Nome do Usuario</th>
                    <th>Endereço de E-mail</th>
                    <th>Ação</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Nome</td>
                        <td>@item.Email</td>
                        <td class="d-flex gap-4">
                            <a class="btn confirm-btn" href="/Usuario/Update?id=@item.Id"> <i class="bi bi-pen"></i>
                                Editar</a>
                            <a class="btn btn-danger" onclick="return window.confirm('Tem a certeza que deseja remover?')"
                                href="/Usuario/Delete?id=@item.Id"><i class="bi bi-trash"></i> Remover</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="row">
        <div class="col-md-6">
            <a class="btn add-btn" href="/Usuario/Add">Adicionar mais Usuarioes</a>
        </div>
    </div>
</div>
